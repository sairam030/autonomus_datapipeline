# =============================================================================
# Autonomous Pipeline — Environment Configuration
# Copy this file to `.env` and fill in your values before running docker compose.
# =============================================================================

# Airflow (required — use `id -u` on Linux to get your UID)
AIRFLOW_UID=1000
AIRFLOW__CORE__FERNET_KEY=UKMzEm3yIuFYEq1y3-2FxPNWSVwRASpahmQ9kQfEr8E=

# PostgreSQL — Metadata DB
POSTGRES_USER=pipeline
POSTGRES_PASSWORD=pipeline123
POSTGRES_DB=autonomous_pipeline
POSTGRES_PORT=5433

# MinIO — Object Storage
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
MINIO_PORT=9010
MINIO_CONSOLE_PORT=9011

# FastAPI Backend
BACKEND_PORT=8000
DEBUG=false
LOG_LEVEL=INFO
CORS_ORIGINS=http://localhost:3001,http://localhost:3000,http://127.0.0.1:3001,http://127.0.0.1:3000

# Optional image overrides (for pull-only deployment from your registry)
PIPELINE_IMAGE=autonomous-pipeline:latest
SPARK_IMAGE=sairam030/custom-airflow-spark:2.9.0

# React Frontend
FRONTEND_PORT=3001

# Spark
SPARK_MASTER_PORT=7078
SPARK_MASTER_WEBUI_PORT=8090
SPARK_WORKER_WEBUI_PORT=8091

# Redis — Caching
REDIS_PORT=6380

# Google Gemini AI (required for AI-powered transformations)
GEMINI_API_KEY=your_gemini_api_key_here
